<% if signed_in? %>
  <h5 class = "comment-title">Add a review:</h5>
  <%= form_for([@product, @product.comments.build], remote: true) do |f| %>
    <p>
      <%= f.label :body, "Comments" %><br>
      <%= f.text_area :body %>
    </p>
    <p>
    <div class="rating"></div>
  </p>
    <p>
      <%= f.submit %>
    </p>
  <% end %>
<% end %>